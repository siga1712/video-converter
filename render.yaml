# Render.com deployment configuration
services:
  - type: web
    name: audioextract
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
